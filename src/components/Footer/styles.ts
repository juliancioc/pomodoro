import styled from 'styled-components'

export const Wrapper = styled.main`
  position: absolute;
  bottom: 30px;

  display: flex;
  align-items: center;

  a {
    text-decoration: none;
    color: #857d8c;
    font-weight: bold;
  }

  p {
    margin-right: 5px;
    font-size: 14px;
  }
`
